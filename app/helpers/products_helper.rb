module ProductsHelper

  # alias for topic_image_path
  def product_image_path(product, options={})
    topic_image_path(product, options)
  end
  
  # alias for topic_image_tag
  def product_image_tag(product, options={})
    topic_image_tag(product, options)
  end

end
