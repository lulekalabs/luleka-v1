Hay una conversación acerca de <%= @kase.tier ? "#{h(@kase.tier.name)}" : '' %>:

<% unless @email.message.blank? %>
---
<%= h(@email.message) %>
---

<%-end-%>

<%= h(@kase.title.capitalize) %> by <%= h(@kase.person.name) %>

<%= h(@kase.description) %>
 
Para ver más detalles, por favor haga clic en el siguiente enlace o copie 
todo el enlace en su navegador:
 
<%= kase_url(@kase) %>

Este mensaje ha sido enviado desde <%= SERVICE_NAME %> - <%= I18n.t('service.tagline') %>.

<%= render :partial => 'notifier/signature' %>
